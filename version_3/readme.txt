find out why everything gets repeated 4 times
where to look:
- parser
- node structure (probably this)

not issue:
implicit tags


issue occurs in (60% sure):
add_text

clean up and add comments, especially HTMLParser


COMPLETED CHAPTER 4
